/// Helper Size class with width & height doubles
class Size {
  double x;
  double y;
  Size(this.x, this.y);
}

/// Helper Point class with x & y axis doubles
class Point {
  double x;
  double y;
  Point(this.x, this.y);
}
